name: sbi_uploader
description: SBI証券CSVをVPSへワンタップアップロード

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  receive_sharing_intent: ^1.8.0
  http: ^1.2.0
  shared_preferences: ^2.2.2
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
